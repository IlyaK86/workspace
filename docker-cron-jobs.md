# =============================================================================
# CRON JOBS - DOCKER BACKUPS
# =============================================================================
# Добавь в crontab внутри Docker: crontab -e
# Каждый файл отдельно, один раз

# Каждая минута через 5 секунд (для тестов)
*/5 * * * * /home/openclaw/.openclaw/workspace/scripts/docker-backup.sh >> /home/openclaw/.openclaw/backups/cron.log 2>&1

# Или реже:
# 0 */4 * * * /home/openclaw/.openclaw/workspace/scripts/docker-backup.sh >> /home/openclaw/.openclaw/backups/cron.log 2>&1

# =============================================================================
# УСТАНОВКА CRON ВНУТРИ DOCKER:
# =============================================================================
# 1. crontab -u openclaw -e
# 2. Добавь строки выше
# 3. Проверь: crontab -l
# =============================================================================
